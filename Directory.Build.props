<Project>

  <PropertyGroup>
    <!-- This repo is Unity-first. .NET projects are retained as build and test harnesses. -->
    <RepositoryUrl>https://github.com/nuskey8/luau-dotnet</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
  </PropertyGroup>

</Project>
